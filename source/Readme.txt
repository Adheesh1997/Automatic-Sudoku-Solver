**** Sudoku solver*****

*sudoku.py and sudoku.ipynb files contain same code.

*We coded this in google colab, so we recommend to run sudoku.ipynb file in jupyter note book or google colab.

*digital_clssifier.h5 contain the model to classify digits in the code, So inthe `load pre-trained model` section insert the path of this model.

*Also we included image for test `testImage.JPEG` you can insert path of this image to test in the `read image` section.